# CareerAgent 面试准备手册

> 用途：用于项目介绍、技术面试、演示前复习和简历措辞校准。
>
> 项目定位：**带职位生命周期治理的本地化求职检索与职业规划 MVP**。它不只是聊天机器人：从多源岗位数据接入、清洗去重、状态管理、混合检索、简历匹配，到受控的大模型工具调用和 Docker 部署，形成完整闭环。

## 0. 当前真实状态：先讲清事实

截至当前本地部署，系统已恢复 **16,967 条岗位记录**：

| 状态 | 数量 | 正确解释 |
| --- | ---: | --- |
| `open` | 2,275 | 入库时来源声明或核验为开放；当前均需要重新同步确认时效 |
| `unverified` | 14,339 | 有岗位记录，但不应被宣传为当前可直接投递的实时岗位 |
| `historical` | 353 | 已关闭或仅供历史参考的岗位 |
| 合计 | 16,967 | 可追溯岗位事实库 |

当前数据大多来自 8 月初的快照或同步记录。健康检查会将超过新鲜度阈值的开放记录标为 stale，因此**不能说“平台今天有 2,275 个正在招聘的岗位”**。

正确表述：

> 系统已经恢复了 1.6967 万条带来源、版本和生命周期状态的岗位记录。当前适合演示数据治理、检索和匹配链路；要作为实时招聘服务对外使用，需要先重新执行官方来源同步和状态核验。

当前项目有 79 个 pytest 用例，覆盖连接器、生命周期、检索、简历解析、门户 API、Agent 工具边界和数据质量等能力。项目依赖检查无冲突。

---

## 1. 一句话定位

> CareerAgent 是一个面向校招、实习和社招用户的本地化求职检索与职业规划系统。它以岗位数据治理为基础，结合 PostgreSQL、Qdrant、混合检索、LangGraph 工作流和受控的本地大模型工具调用，为用户提供可追溯的岗位搜索、简历匹配和准备建议。

---

## 2. 可直接背诵的项目介绍

### 2.1 30 秒版本

> 我做的是 CareerAgent，一个面向校招、实习和社招场景的求职检索与职业规划系统。核心难点不是单纯调用大模型，而是解决职位数据分散、状态容易过期、搜索不准，以及简历和岗位要求难以对齐的问题。
>
> 系统使用 FastAPI 提供服务，PostgreSQL 管理结构化岗位和版本历史，Qdrant 做向量检索；通过职位生命周期机制区分开放、待核验、缺失和历史岗位。检索采用结构化筛选、精确关键词、向量召回和时效重排结合的混合方案。大模型部分使用本地 Ollama 和 Qwen3 4B，但只允许调用受限的只读工具，并且调用失败会自动降级到确定性规则流程。

### 2.2 2 分钟版本

> CareerAgent 的目标是帮助求职者更可靠地找到职位、理解职位要求，并基于自己的简历做准备，而不是简单生成一段泛泛的职业建议。
>
> 项目分为四层。
>
> 第一层是数据层。我将不同来源的招聘数据标准化为统一岗位模型，包括公司、职位、地点、来源链接、外部职位 ID、职位描述、届别、岗位类别、首次发现时间、最后发现时间和生命周期状态。系统不会因为一个快照里没有某岗位就贸然认定岗位关闭；只有能够完整枚举职位的权威来源，连续缺失达到阈值后才会归档。
>
> 第二层是检索层。PostgreSQL 是事实主库，保存岗位、版本、同步任务和来源健康度；Qdrant 保存岗位向量，并将实时岗位和历史岗位拆到两个集合。用户搜索时先做公司、岗位、地点、届别等硬过滤，再做关键词精确匹配；只有用户允许扩大语义范围时，才走向量召回。最后用语义相关度、词法命中和最近核验时间进行重排。
>
> 第三层是求职辅助层。系统支持 TXT 和 DOCX 简历解析，从简历和 JD 中提取可解释的技能标签，给出已覆盖技能、待补证据和面试准备动作。这个分数代表准备度，不代表录用概率。
>
> 第四层是 Agent 层。项目中有一个 LangGraph 的确定性工作流，负责输入校验、岗位匹配、可选面试题生成和质量审查。同时我接入了本地 Qwen3 4B 的工具调用能力：模型只能搜索本地岗位、读取岗位详情、做简历匹配和查看来源时效，不能投递、删库或修改职位。这样既利用了大模型的表达和规划能力，又保证岗位事实仍来自可追溯的数据。

---

## 3. 项目要解决的问题

普通招聘搜索有四个典型问题：

1. **岗位来源分散**：公司官网、Greenhouse、Ashby、Lever、招聘平台和历史数据格式不同。
2. **岗位状态不可靠**：页面存在不代表岗位仍可投递；一次抓取列表中没有某岗位也不代表它已经关闭。
3. **搜索结果不准**：搜索“产品”可能出现“产品研发工程师”；纯语义搜索又可能过度泛化。
4. **求职建议不可追溯**：大模型可能编造职位、链接、截止时间、要求或录用概率。

项目的关键思路是：

> 先把岗位事实治理清楚，再让检索、匹配和大模型围绕这些事实工作。

---

## 4. 总体架构

```text
公开招聘源 / 官方 ATS / 已审核快照
              │
              ▼
     连接器、清洗、标准化、去重
              │
              ▼
  岗位生命周期治理 + 同步审计 + 来源健康
              │
     ┌────────┴─────────┐
     ▼                  ▼
PostgreSQL          Qdrant
事实主库             向量检索库
岗位/版本/同步记录     实时、历史双集合
     │                  │
     └────────┬─────────┘
              ▼
      混合检索与可解释重排
              │
     ┌────────┼─────────┐
     ▼        ▼         ▼
岗位搜索   简历匹配   求职规划 Copilot
                         │
                         ▼
     可选：本地 Ollama / Qwen3 4B 工具调用
                         │
                         ▼
              FastAPI + Web 页面 + OpenAPI
```

Docker Compose 运行三个服务：

```text
career-platform：FastAPI 应用，暴露 8000 端口
postgres：结构化数据、版本和审计记录
qdrant：向量索引
```

Ollama 不在 Compose 中运行，而是在宿主机单独运行。应用容器通过 `host.docker.internal:11434` 调用本机 Ollama，因此不会将模型权重重复打包进应用镜像。

---

## 5. 数据层：职位生命周期治理

### 5.1 统一岗位模型

每条岗位包含：

- `source_name`：来源名称，例如 Greenhouse、Ashby、快照导入源；
- `external_id`：来源系统稳定职位 ID；
- `source_url`：官方详情或投递链接；
- `company`、`title`、`location`；
- `description`：清洗后的岗位描述；
- `job_group`：岗位族，例如工程、产品、数据、设计；
- `employment_kind`：校招、实习、社招等；
- `campus_cycle`：例如 2027 届；
- `lifecycle_status`：职位生命周期状态；
- `first_seen_at`、`last_seen_at`、`last_verified_at`；
- `content_hash`：判断岗位内容是否实质改变；
- `metadata_json`：保留来源特有字段。

数据库对 `(source_name, external_id)` 建立唯一约束，避免同一来源的同一职位重复写入。

### 5.2 内容哈希与版本审计

每次同步会基于标题、公司、地点、描述、来源 URL 和来源声明状态计算 SHA-256：

- 哈希相同：岗位没有实质变化；
- 哈希不同：职位描述、地点、状态或链接发生变化；
- 发生变化：写入新的 `job_versions` 版本记录。

这使系统能够回答：

- 岗位何时变更；
- 之前的 JD 是什么；
- 岗位为何从开放变为历史；
- 数据是何时发现和何时核验的。

### 5.3 生命周期状态

| 状态 | 含义 | 是否适合直接投递 |
| --- | --- | --- |
| `open` | 来源声明开放，或经权威来源核验开放 | 可以，但仍应打开官方链接确认 |
| `unverified` | 有记录，但无法证明当前仍开放 | 不可当作实时岗位宣传 |
| `missing` | 权威来源本次未发现，尚未达到关闭阈值 | 暂不建议投递 |
| `historical` | 来源声明关闭，或权威来源连续缺失后归档 | 仅供趋势和要求参考 |

最关键的规则：

> 只有 `authoritative=True` 的权威来源，才允许在连续缺失后自动归档岗位。

例如，能完整枚举当前职位的 Greenhouse 公共 API 可以用于关闭判断。JSONL 历史快照、只展示首屏的招聘网页、不能确定分页完整性的页面则是非权威来源：看不到岗位不代表岗位关闭。

### 5.4 同步、重试与清理

- 每次同步创建 `sync_runs` 审计记录；
- 每个来源记录最后尝试时间、最后成功时间、失败次数、错误和抓取数量；
- 已配置的连接器会经过重试包装；
- 调度器使用 APScheduler，每日按配置时刻执行，`max_instances=1` 且启用 `coalesce`，避免任务重叠堆积；
- 历史岗位保留期默认 1,095 天，之后可按规则清理。

---

## 6. PostgreSQL 与 Qdrant 的职责

### PostgreSQL：事实主库

PostgreSQL 保存：

- 当前岗位记录；
- 岗位版本历史；
- 同步任务；
- 来源健康；
- 收藏、投递阶段、订阅、用户偏好等工作台数据。

它负责事务、一致性、唯一约束、审计和结构化查询。

### Qdrant：语义检索读模型

Qdrant 保存岗位的向量表示。Embedding 文本由职位标题、公司、岗位族和岗位描述组成，使用 FastEmbed 与 `BAAI/bge-small-zh-v1.5`。

项目将向量拆为两个集合：

- `career_agent_live_jobs_v1`
- `career_agent_history_jobs_v1`

好处：默认求职搜索不会被大量历史岗位干扰，同时仍可让用户查询历史岗位做技能趋势分析。

### 面试回答：为什么不是只用向量数据库？

> 向量数据库适合语义召回，但不适合作为唯一事实库。岗位的唯一约束、同步事务、版本审计、来源健康和生命周期状态需要关系数据库保障。因此 PostgreSQL 是 source of truth，Qdrant 是可重建的检索读模型。

### 当前注意事项

SQLite 历史记录已安全迁移至 PostgreSQL。迁移后，数据库精确搜索和结构化筛选可用；但正式对外使用前，还应执行一次 Qdrant 全量重建，使全部历史数据获得完整向量索引覆盖。

---

## 7. 检索链路：混合检索而非单一向量搜索

### 7.1 默认精确优先

默认搜索顺序：

1. 公司精确匹配；
2. 岗位标题或岗位类别精确匹配；
3. 全文关键词匹配；
4. 结构化过滤：地点、届别、岗位类型、岗位族、状态范围。

系统维护中英别名，例如：

```text
后端 → backend / back-end
前端 → frontend / front-end
产品经理 → product manager / product owner
```

例如搜索“后端”时，会优先约束为工程类岗位，避免把“产品后端支持”等不相关职位排在前面。

### 7.2 语义扩展由用户显式开启

只有在 `expand_semantic=true` 时，系统才使用 Qdrant 扩大语义候选集。

这样避免两类常见问题：

- 用户要精确找“Python 后端”，语义搜索却给出“数据分析”或“算法工程”；
- 用户搜索公司名，语义相似但属于其他公司的岗位被混入结果。

### 7.3 重排逻辑

最终分数由三部分组成：

```text
最终分数 =
0.62 × 语义相关度
+ 0.28 × 关键词命中度
+ 0.10 × 时效分
```

时效分基于 `last_verified_at` 或 `last_seen_at` 衰减。

### 面试回答：为什么不能只按向量相似度排序？

> 向量检索擅长召回语义相近内容，但公司名、岗位名、届别等硬约束并不可靠。我先用结构化过滤保证精确性，再通过向量扩大召回，最后用词法和时效重排，在精度、召回和新鲜度之间取得平衡。

### 7.4 跨来源重复岗位

不同来源可能出现同一公司、同一职位。展示层以“规范化公司名 + 规范化职位名”作为候选聚类键：

```text
company_normalized :: title_normalized
```

搜索页只展示一个代表记录，但数据库保留全部原始来源和职位记录，以便审计和回溯。

---

## 8. 简历解析与岗位匹配

### 8.1 支持范围

支持：

- TXT；
- Markdown；
- DOCX。

限制：

- 文件不超过 2 MB；
- 当前不支持 PDF，系统会明确要求用户导出为 DOCX/TXT 或粘贴文本，而不是假装解析成功。

### 8.2 匹配逻辑

系统从简历和 JD 中抽取可解释技能标签，例如：

- Python、Java、Go、C++、SQL；
- Docker、Kubernetes、CI/CD；
- React、Vue、TypeScript；
- LLM、RAG、机器学习；
- 数据分析、产品、项目管理等。

输出：

- 已覆盖技能；
- 岗位文本识别到的技能；
- 需要补齐的技能证据或项目证据；
- 准备行动建议；
- 准备度分数。

### 8.3 分数边界

> 分数不是录用概率，而是岗位文本中可识别技能与简历中可识别技能的覆盖程度。

正确回答：

> 录用结果还受项目深度、学历、业务经验、竞争情况、沟通和面试表现影响。当前系统只对文本中可解释的技能证据负责，因此把结果定义为准备度提示，而不是录用预测。

---

## 9. LangGraph 与本地 LLM Agent：必须区分

### 9.1 LangGraph 确定性工作流

项目中存在如下确定性 LangGraph 图：

```text
输入校验
   ↓
岗位匹配
   ↓
是否需要面试准备？
   ├─ 是：生成面试问题
   └─ 否：跳过
   ↓
质量审查 Critic
   ↓
输出结果与 trace
```

节点：

- `input_validation`；
- `match_agent`；
- `interview_agent`；
- `critic`。

每个节点记录执行耗时，最终输出 trace。

正确表述：

> 我用 LangGraph 管理有状态的业务流程和条件路由，使匹配、面试题和质量检查具备清晰的节点边界与执行轨迹，后续也便于增加流式输出、人工审核和持久化。

### 9.2 可选的本地模型工具调用

项目另有 Tool Calling Copilot，可调用宿主机 Ollama 中的 `career-qwen3:4b`。

调用需同时满足：

- 后端开启 LLM；
- 请求设置 `use_llm=true`；
- 即用户明确同意本次模型调用。

模型工具白名单：

| 工具 | 能力 |
| --- | --- |
| `search_jobs` | 搜索本地版本化岗位 |
| `get_job_details` | 获取已搜索到岗位的详情 |
| `analyze_resume_for_jobs` | 对指定岗位做简历准备度分析 |
| `get_source_freshness` | 查询来源健康和核验时效 |

模型不能：

- 投递简历；
- 修改岗位状态；
- 删除数据；
- 写入数据库；
- 绕过登录、反爬或受保护接口；
- 凭空生成岗位、URL、截止时间或录用结论。

安全限制：

- 最多 4 轮工具调用；
- 最多 8 次工具调用；
- 默认输出最多 128 tokens；
- 调用超时上限 120 秒；
- 工具 trace 不写入完整简历正文；
- 未配置、未同意或失败时，自动降级到确定性 Copilot。

### 面试回答：如何减少大模型幻觉？

> 模型没有自由访问互联网或直接写数据库的能力，只能调用四个只读本地工具。岗位推荐前必须读取岗位详情；系统提示要求引用 job ID；调用轮数和输出长度都有上限。模型未开启、用户未同意或调用失败时，系统回退到确定性匹配流程。

### 重要：不要夸大

不要说“多 Agent 自主协商系统”。

准确说法：

> 项目包含 LangGraph 多节点业务工作流，以及一个受控的工具调用式 LLM Agent。它是 Agentic RAG/工具调用式求职助手，不是多个 LLM 自主协商的 multi-agent 系统。

---

## 10. 本地 Qwen3 4B 部署

当前项目保留的模型标签：

```text
career-qwen3:4b
```

此前两个约 2.5 GB 的 Qwen 标签实际共享底层模型 Blob，并非完整存储了两份权重；冗余标签已删除，只保留项目使用的自定义标签。

本地模型的职责：

- 理解用户目标；
- 选择调用受限工具；
- 基于工具证据组织中文求职建议；
- 给出下一步行动。

它不负责：

- 保存岗位真相；
- 决定职位是否开放；
- 判断录用概率；
- 替代数据库和检索。

本地部署优点：

- 简历可不发送给外部模型服务；
- 不依赖云端 API Key；
- 成本和版本可控；
- 可离线演示。

局限：

- 4B 模型推理稳定性和复杂能力弱于更大模型；
- 推理速度取决于本机硬件；
- 仍需通过人工评测集验证工具调用成功率、引用完整性和建议质量。

---

## 11. FastAPI 接口与产品能力

| 类型 | 典型接口 | 作用 |
| --- | --- | --- |
| 健康检查 | `/health` | 服务、数据库、调度器、岗位统计 |
| 管理后台 | `/admin` | 来源健康、概览、手动同步 |
| 数据质量 | `/api/data-quality` | 过期岗位、缺失链接、分类质量、来源告警 |
| 职位搜索 | `/api/career/search` | 精确/语义搜索和条件筛选 |
| 职位详情 | `/api/career/jobs/{job_id}` | JD、来源和职位详情 |
| 简历匹配 | `/api/career/match` | 技能覆盖、缺口、准备动作 |
| 职业规划 | `/api/career/copilot/plan` | 确定性或本地 LLM 工具调用方案 |
| 个人工作台 | 收藏、投递、订阅、Profile 接口 | 求职流程管理 |

投递阶段：

```text
saved → applied → assessment → interview → offer / rejected / archived
```

订阅在未配置 SMTP 时只生成预览并记录状态，不会宣称邮件已经发送。

---

## 12. Docker、端口与本地访问

Docker Compose 运行：

- FastAPI 应用；
- PostgreSQL 16；
- Qdrant 1.16.2。

端口映射：

```text
0.0.0.0:8000 -> Docker 容器 FastAPI 的 8000
```

网络含义：

- `http://127.0.0.1:8000`：仅代表当前这台电脑；
- 其他电脑访问 `127.0.0.1:8000`，访问的是它们自己的电脑；
- 同一局域网设备可使用本机局域网 IP 加 `:8000`，前提是同网段、Windows 防火墙允许且网络不隔离客户端；
- 这不是公网部署，外网不能直接访问。

### 面试回答：本地部署和生产部署差别

> 本地部署解决开发、演示和隐私问题；生产部署还需要补齐登录认证、密钥管理、HTTPS、反向代理、日志聚合、监控告警、数据库备份、限流、异步任务解耦和横向扩容。

---

## 13. SQLite 到 PostgreSQL 的安全迁移

旧 SQLite 数据库中保留了约 1.7 万条岗位；Docker Compose 新建 PostgreSQL 卷后，服务最初显示空库。

迁移脚本的保护策略：

1. SQLite 源库以只读方式打开；
2. PostgreSQL 目标库必须为空，否则拒绝覆盖；
3. 按 500 条为一批迁移；
4. 保留岗位 ID、历史版本、来源健康和同步审计；
5. 重置 PostgreSQL 自增序列；
6. 对比源、目标每张表的记录数，不一致即报错。

迁移的表：

- `job_records`；
- `job_versions`；
- `source_health`；
- `sync_runs`。

面试表述：

> 我没有做无保护的导出导入，而是将迁移封装为一次性、带 guard 的脚本：源库只读、目标非空拒绝写入、分批写入、迁移后逐表计数校验。这样避免覆盖运行中的数据，同时完整保留岗位版本和审计信息。

---

## 14. 测试与评测

当前 79 个 pytest 用例覆盖：

- Agent 工具边界；
- 岗位生命周期状态迁移；
- 数据源连接器；
- 同步失败重试；
- Qdrant 索引增量更新；
- 检索精度与岗位意图；
- 简历解析；
- FastAPI 门户接口；
- 官方来源契约；
- 快照核验与数据质量；
- LangGraph 工作流。

项目还提供 JSONL 格式人工评测模板，可评测：

- 公司精确检索；
- 岗位族意图识别；
- 语义检索；
- 地点、届别、状态过滤；
- Agent 引用完整率；
- 时效表述错误率。

### 诚实边界

没有完成足够规模人工标注和基线报告前，不应声称：

- Recall 提升百分比；
- MRR 指标；
- 意图准确率；
- 模型准确率达到某个具体数字。

正确表述：

> 我已经搭建了自动化测试与人工评测数据格式，但大规模效果指标仍是下一阶段工作，避免在没有标注集时虚构模型效果。

---

## 15. 高频面试问答

### Q1：为什么不用纯大模型？

> 大模型不能证明岗位是否存在、是否开放、链接是否真实。求职场景的事实性和时效性比文案生成更重要，所以我先构建岗位事实库、版本和状态治理，再把 LLM 限制在工具调用和建议生成层。

### Q2：为什么选择 PostgreSQL + Qdrant？

> PostgreSQL 负责事务、审计、结构化过滤和关系数据；Qdrant 专注语义向量检索。二者职责清晰。后续大规模全文检索可继续引入 PostgreSQL FTS、BM25 或 Qdrant sparse vector。

### Q3：为什么要将实时和历史岗位分集合？

> 历史岗位对职业研究有价值，但会污染实时求职搜索。拆分集合后，默认查询只看实时集合，历史检索由用户明确选择。

### Q4：一个岗位没抓到，怎么判断它关闭了？

> 先看来源能否完整枚举职位。只有权威来源且岗位连续缺失达到阈值时，才归档；非权威来源和快照源不以“未看到”作为关闭证据。

### Q5：为什么向量检索还需要关键词？

> 向量检索提高召回，但公司名、岗位名、届别这些硬约束需要精确匹配。我的策略是结构化硬过滤和精确匹配优先，语义扩展由用户主动开启，最后做词法、语义和时效重排。

### Q6：匹配分数为何不是录用概率？

> 录用概率需要真实招聘结果、用户行为和面试表现等标签。当前只基于简历和 JD 的可解释技能证据，因此定义为准备度，而不是录用预测。

### Q7：为什么使用 LangGraph？

> LangGraph 使节点、状态和条件路由显式化，便于 trace、人工审核、流式输出和后续持久化，而不是把流程逻辑堆在一个函数里。

### Q8：这是否属于多 Agent？

> 严格说不是多 LLM 自主协商系统。项目有 LangGraph 多节点业务工作流和一个工具调用 Agent。我会称为 Agentic RAG/工具调用式求职助手，而不是夸大为多 Agent 协作。

### Q9：怎么减少大模型幻觉？

> 模型只能调用四个只读本地工具，无法直接写数据库或访问任意网页；岗位推荐前应读取详情；输出要求引用 job ID；调用轮数和输出长度受限；失败时回退到确定性流程。

### Q10：为什么选择本地 Qwen3 4B？

> 本地模型适合简历这种隐私敏感场景，可避免依赖 API Key，成本和部署更可控。4B 模型负责工具编排和简短建议；岗位事实、状态和匹配仍由数据库与确定性逻辑保证。

### Q11：数据如何保证新鲜度？

> 岗位记录首次发现、最后发现和最后核验时间；来源记录最后成功同步、失败次数和错误信息；数据质量接口会报告过期开放岗位和过期来源。

### Q12：当前最大的技术债是什么？

> 第一，当前导入数据需要重新跑官方同步恢复时效；第二，历史数据迁移后应全量重建 Qdrant 向量索引；第三，账户体系、生产监控与大规模人工评测尚未完成。

### Q13：如果数据规模达到千万级怎么办？

> 将同步任务异步队列化，PostgreSQL 按来源或时间分区，检索增加全文/稀疏检索和分片策略，向量索引改为批量构建与增量更新，并将在线查询和离线同步、Embedding 生成解耦。

### Q14：简历数据如何保护？

> 默认使用本地模型；工具轨迹不保存完整简历正文；模型没有写库工具。生产化时还需要增加登录认证、字段加密、访问审计、数据保留策略和用户删除机制。

---

## 16. 5 分钟现场演示顺序

1. 打开主页：`http://127.0.0.1:8000`。
2. 先讲问题：不是从聊天机器人开始，而是先解决岗位数据可信度和时效问题。
3. 演示搜索：关键词、公司、地点、届别、状态范围；说明精确优先和语义扩展的区别。
4. 点击职位详情：展示 JD、来源链接和生命周期字段，强调官方链接才是投递事实依据。
5. 演示简历匹配：使用不含个人隐私的示例简历，展示技能覆盖、缺口与准备动作；强调不是录用概率。
6. 演示 Copilot：先展示确定性结果，再说明勾选本地模型后只能调用本地只读工具。
7. 打开 `/health` 或管理后台：展示数据库后端、岗位数量、来源健康和数据质量。
8. 主动说明边界：当前数据需要重新同步，向量库需要全量重建后再作对外实时展示。

主动说明边界不是扣分，反而体现工程成熟度。

---

## 17. 简历描述建议

可使用：

- 设计并实现 CareerAgent 本地化求职检索平台，基于 FastAPI、PostgreSQL、Qdrant 与 Docker Compose 构建岗位数据治理、混合检索、简历匹配及求职工作台闭环。
- 构建职位生命周期与版本审计机制，基于来源权威性、连续缺失阈值和内容哈希处理增量同步、状态核验与历史归档。
- 实现结构化过滤、关键词精确匹配、向量召回与时效重排的混合检索链路，并支持实时/历史职位双索引。
- 使用 LangGraph 编排可追溯的简历—JD 匹配与面试准备工作流；接入本地 Qwen3 模型，实现受限只读工具调用、证据引用和确定性降级。
- 编写 79 个自动化测试，覆盖连接器、生命周期、检索、门户 API、简历解析和 Agent 工具边界。

不要使用：

- “实现多 Agent 自主协作系统”；
- “覆盖 100+ 家企业实时岗位”；
- “大模型保证岗位真实可靠”；
- “匹配分数预测录用概率”；
- “模型准确率达到 XX%”。

---

## 18. 核心知识点速记

| 概念 | 面试中可用的解释 |
| --- | --- |
| FastAPI | Python Web 框架，用来提供网页和 API |
| PostgreSQL | 关系数据库，保存事实、状态、版本和审计记录 |
| Qdrant | 向量数据库，保存 Embedding，用于语义相似搜索 |
| Embedding | 将文本编码成向量，使语义相近的表达可以互相检索 |
| 混合检索 | 关键词、结构化筛选、向量检索和重排结合 |
| RAG | 先从知识库检索事实，再让模型基于事实生成回答 |
| Tool Calling | 模型选择调用受限工具，而不是自由生成所有事实 |
| LangGraph | 用节点和状态转移图组织复杂 AI/业务流程 |
| Docker Compose | 一次启动多个互相依赖的服务，例如应用、数据库、向量库 |
| 生命周期治理 | 记录数据何时出现、何时变化、何时过期、为何归档 |

---

## 19. 最后一分钟总结

> 这个项目最核心的价值不是“接了一个大模型”，而是把求职场景中最难且最容易被忽视的问题——数据来源、岗位时效、状态治理、检索准确性和建议可追溯性——做成了一套工程闭环。大模型在其中是受控增强层，不是事实来源。这样既能给用户更自然的求职规划体验，又能尽量避免把过期岗位、虚构信息或不可解释的匹配结果带给用户。
