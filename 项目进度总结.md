# CareerAgent 项目进度总结

> 更新时间：2026-08-15  
> 项目定位：面向校招、社招与实习求职场景的多源岗位检索与职业规划 Agent

## 0. 2026-08-15 官方中文招聘源登记更新

- 已将华为、OPPO、Bambu Lab、百度、拼多多、DJI、智元机器人、普渡科技、科大讯飞、Ubiquant、Shopee、JoinQuant、NVIDIA、联想和腾讯音乐的企业官网入口登记到来源目录；目录只保存公开招聘页，不保存个人中心、投递记录、内推码或分享令牌。
- 百度 2027 届校招公开 SSR 页已作为非权威连接器随默认环境配置启用。2026-08-15 的只读核验可读取当前页面可见的 10 条岗位；首次管理员同步会将其写入实时索引，但页面首屏不代表完整职位集合，因此不会触发自动关闭。
- 拼多多校招公开分页接口已作为非权威连接器随默认环境配置启用。2026-08-15 的端到端同步核验抓取并写入 22 条开放岗位，包含职位 ID、名称、职责、地点和投递链接；在确认“列表消失”等同于“职位关闭”前，同样不触发自动归档。
- Shopee 官方 Moka 公开招聘接口已作为权威连接器随默认环境配置启用。2026-08-15 的端到端同步核验分别抓取并写入校招 46 条、AI Star 人才计划 29 条、研发中心实习生招聘 49 条，共 124 条开放岗位；均使用公开分页列表、稳定职位 ID 与官方详情链接，不使用内推码、候选人数据或投递接口。Shopee 校招首页公开链接只指向这三个招聘子站点；“人才计划实习生”没有独立第四站点，若官网将其发布到 AI Star 或实习生板块，会在这两条同步链路中一并入库。
- JoinQuant 与 NVIDIA 官方 Moka 公开招聘接口已作为权威连接器随默认环境配置启用。2026-08-15 的端到端同步核验分别抓取并写入 JoinQuant 27 条、NVIDIA 30 条开放岗位；JoinQuant 覆盖职能、投研和技术类，NVIDIA 覆盖 2027 校招 14 条与实习 16 条。两者均使用完整公开分页列表、稳定职位 ID 和官方详情链接。
- 联想中国官网公开职位接口已作为权威连接器随默认环境配置启用。2026-08-15 的端到端同步核验抓取并写入 90 条开放岗位；每条均保留稳定职位 ID、完整职责与任职要求、官网详情链接及官方返回的工作地点代码。官网列表未返回地点文本时不猜测城市名称。
- 华为、OPPO、Bambu Lab、DJI、智元机器人、普渡科技、科大讯飞、Ubiquant 和腾讯音乐目前仍仅确认“企业自有招聘域名”。在各自完成公开列表、稳定职位 ID、分页、fixture、来源契约和关闭语义验收前，保持禁用，避免把个人投递页或不完整页面错误当作实时数据源。

## 1. 项目目标

CareerAgent 旨在解决岗位信息来源分散、招聘状态难追踪、JD 检索不准确，以及用户难以将自身经历与岗位要求有效对齐的问题。系统将多源岗位采集、数据治理、实时/历史职位检索、简历解析、岗位匹配和能力提升建议整合为统一的 Agent 应用。

核心使用路径为：

1. 定时同步公开招聘数据并维护岗位生命周期；
2. 用户按公司、岗位、城市、届别、岗位类型等条件检索；
3. 系统理解 JD 与用户简历，输出匹配度、匹配依据、技能缺口和准备建议；
4. 管理端监控数据源健康度、同步任务、数据质量和异常重试情况。

## 2. 已完成内容

### 2.1 多源岗位数据与生命周期管理

- 实现可插拔的数据采集框架，支持接入国内公开招聘页面与海外官方招聘系统等多类来源。
- 建立统一岗位数据模型，覆盖公司、岗位名称、地点、岗位描述、岗位类型、届别、来源、发布时间、招聘状态和数据版本等字段。
- 实现岗位去重、增量同步、状态关闭检测、历史归档与过期数据清理。
- 支持校招届别识别，并完成校招、社招、实习岗位分类；无法自动确认的记录进入待核验队列，而非直接归入历史数据。
- 已沉淀约 **1.7 万条**岗位 JD，覆盖 **100+** 家企业招聘渠道。

### 2.2 数据库、向量检索与搜索准确性优化

- 使用 PostgreSQL 保存结构化岗位信息、岗位版本、采集任务、来源健康度与核验状态。
- 使用 Qdrant 构建“实时岗位 / 历史岗位”双向量索引，避免已结束岗位干扰实时求职搜索，同时保留历史岗位参考价值。
- 接入 Embedding 向量化能力，支持职位语义召回。
- 搜索链路采用“结构化硬过滤 + 关键词精确匹配 + 语义召回”的组合策略：
  - 公司检索优先执行公司名称精确/别名匹配，避免搜索“华为”时返回无关企业岗位；
  - 岗位检索结合岗位职责与技能要求判断，避免仅因标题包含“产品 / Product”等词就被错误归类；
  - 支持按公司、岗位、城市、届别、岗位类型、招聘状态等条件组合筛选；
  - 解除前端固定 20 条结果限制，返回全部符合筛选条件的数据。
- 清洗 JD 中的 HTML、富文本残留与实体编码，避免搜索结果摘要出现 `<p>`、`<span>` 等代码片段。

### 2.3 职业规划 Agent

- 基于 LangGraph 构建多节点工作流，串联 JD 理解、简历解析、岗位匹配、技能缺口分析和学习建议生成。
- 可针对不同届别与目标岗位输出：岗位匹配度、关键匹配依据、缺失技能、优先准备方向与行动建议。
- 支持将用户简历中的技能、项目经历与目标 JD 做结构化对齐，为后续的简历优化、投递优先级排序和面试题生成提供基础。

### 2.4 服务化、部署与可观测性

- 使用 FastAPI 提供岗位搜索、岗位详情、数据管理和 Agent 分析接口，并完成本地 Web 页面联调。
- 建立管理后台能力，包括数据质量报告、采集任务记录、来源健康监控、失败重试和待核验数据管理。
- 使用 Docker Compose 容器化应用、PostgreSQL 与 Qdrant；Docker Desktop 与 WSL 环境已完成配置，数据盘迁移已成功。
- 已补充自动化测试，核心流程当前覆盖 **66 项**测试用例。

## 3. 当前可用状态

目前项目已具备本地可运行、可检索、可管理的完整闭环：

- Web 服务可通过 `http://127.0.0.1:8000/` 访问；
- 可搜索当前岗位数据，并查看岗位摘要与详情；
- PostgreSQL 与 Qdrant 可通过 Docker 持久化保存数据与索引；
- 支持实时岗位与历史岗位分开检索；
- 数据源同步、状态管理、历史归档、来源监控与失败重试已有工程化实现；
- Agent 分析链路可基于简历和 JD 输出匹配及准备建议。

## 4. 当前数据状态与注意事项

当前数据集同时包含实时、待核验与历史岗位。待核验不等价于“已结束”：它表示数据源暂未给出足够明确的职位关闭证据，需要继续通过后续采集、页面状态或人工审核确认。

后续使用中应遵循以下规则：

- 面向求职者的默认搜索优先展示 `开放中` 岗位；
- `待核验` 可作为补充信息展示，并标明状态，避免被误认为仍在招聘；
- 已关闭岗位转入历史索引，供岗位趋势、技能要求和历年招聘节奏分析使用；
- 超过保留期限的历史数据按规则清理，控制存储成本并避免陈旧信息误导用户。

## 5. 尚可继续完善的方向

### 5.1 数据覆盖与核验能力

- 持续扩展可稳定访问的企业招聘来源，提高中文实时校招岗位覆盖率。
- 对不同来源完善关闭状态判断规则，例如职位页 404、职位下线标记、接口返回状态变化与连续多次采集缺失。
- 增加人工审核工作台，支持批量确认、驳回、标注证据与审核日志，逐步减少待核验数据。

### 5.2 检索与匹配效果

- 引入 Reranker 重排模型，对初步召回的岗位进行精排，进一步提升复杂岗位搜索的准确率。
- 建立岗位标准分类体系与同义词库，例如区分“产品经理”“产品运营”“产品设计”“商业化产品”等相近但职责不同的岗位。
- 构建检索评测集，持续跟踪公司精确检索率、岗位分类准确率、召回率和用户点击反馈。

### 5.3 Agent 能力扩展

- 增加简历改写、项目经历优化、投递优先级排序、模拟面试与面试复盘能力。
- 引入用户偏好和投递记录，形成长期职业规划记忆与个性化推荐。
- 接入大模型评测与安全策略，监控建议质量、事实一致性和输出稳定性。

### 5.4 生产化部署

- 部署到云服务器，配置域名、HTTPS、日志聚合、备份与告警。
- 通过定时任务或 CI/CD 自动执行每日同步、索引更新、数据质量检查和部署。
- 增加账号系统、访问权限和限流机制，支持多人使用与运营管理。

## 6. 下一阶段优先级

1. 扩充并稳定中文官方招聘数据源，完善状态核验规则；
2. 建立岗位分类词典与检索评测集，继续优化精确筛选与语义排序；
3. 完善简历上传、岗位对比、学习计划和投递管理等用户侧闭环；
4. 完成云端部署、每日自动同步、数据备份与运行告警；
5. 录制演示视频、补充架构图与项目文档，用于简历投递和面试展示。

## 7. 简历可提炼的能力点

- Agent 工作流编排：LangGraph / LangChain 多节点任务协同；
- RAG 与向量检索：Embedding、Qdrant、结构化过滤、混合检索；
- 数据工程：多源采集、去重、生命周期管理、数据质量治理；
- 后端工程：Python、FastAPI、PostgreSQL、SQLAlchemy、异步任务；
- 工程化部署：Docker Compose、服务容器化、任务监控、失败重试与自动化测试。
