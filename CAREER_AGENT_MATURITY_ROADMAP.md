# CareerAgent：成熟度评估、真实边界与升级路线

> 本文只记录已实现能力与可验收计划，不把“计划开发”写成“已经上线”。

## 当前结论

CareerAgent 已经不是 Toy 项目，而是可运行的“带数据生命周期治理的求职检索产品 MVP”。它拥有真实岗位源、PostgreSQL、Qdrant、Docker、增量同步、失败重试、历史归档、检索/匹配和用户工作台闭环。

当前准确定位是 **RAG/向量检索驱动的求职助手**。旧 Copilot 主要使用向量检索、确定性重排、技能词表与规则建议，因此此前不能仅因为项目名含 Agent 就宣称“大模型 Agent”。本次已加入可选的真实 LLM 函数工具调用实现；实际配置、勾选同意并完成验收后，才适合在简历中写 LLM Agent。

| 维度 | 当前判断 | 说明 |
| --- | --- | --- |
| 工程闭环 | 较强 | Docker + PostgreSQL + Qdrant；同步、重试、来源健康、版本和状态治理 |
| 数据可信度 | 中上 | 已验证源可同步；快照、动态页面和待核验来源不伪装成实时岗位 |
| 检索与匹配 | 较强 | 公司/岗位精确优先、向量召回、词法/新鲜度重排、届别/岗位族过滤、技能缺口 |
| 产品体验 | 中上 | 搜索、详情、官方跳转、简历解析、收藏、投递进度和订阅 |
| Agent | 已实现可选工具调用，待真实配置和评测 | 有受限工具、引用白名单、轨迹和确定性降级 |
| 国内实时源广度 | 待增强 | 快手已验证；百度公开 SSR 首屏与拼多多公开分页岗位接口已启用为非权威源；联想中国官网，以及 Shopee 的校招、AI Star 人才计划和研发中心实习生招聘，JoinQuant、NVIDIA 的公开招聘板块已启用为权威公开分页源；华为、OPPO、Bambu Lab、DJI、智元机器人、普渡科技、科大讯飞、Ubiquant 和腾讯音乐已登记，仍须逐源确认公开读取契约 |

## 已经足以说明“不 Toy”的部分

1. 每条岗位有来源、外部 ID、版本、首末发现时间、生命周期状态和历史记录，不是静态 JSON 演示。
2. 只有可完整枚举岗位的权威源才会因连续缺失被归档；未验证源不会被错误标成关闭。
3. 搜索不是简单字符串匹配：实时/历史索引分离，向量召回、词法意图和最近核验时间共同参与排序。
4. 用户可以从岗位搜索进入官方页，并完成简历匹配、收藏、投递阶段和订阅等动作。
5. 项目已有连接器、检索精度、生命周期、门户 API、简历解析和数据质量相关测试。

## 仍不能夸大的地方

- 不能把所有收录公司写成“实时校招”；海外 ATS、历史快照和待核验网页必须如实标注。
- 在真实模型配置和验收前，不要写“多 Agent 协作”或“LLM Agent”。
- 当前个人工作台不是完整多用户账号系统；未配 SMTP 时订阅只生成预览。
- 未完成人工标注评测前，不要写 Recall、MRR、意图准确率或“提升 X%”。

## 新增：真实 LLM 工具调用工作流

```text
用户目标 + 可选简历
  → 用户显式同意外部模型调用
  → 本地 Ollama Responses API
      → search_jobs（本地岗位检索）
      → get_job_details（JD 与官方链接）
      → analyze_resume_for_jobs（可解释技能差距）
      → get_source_freshness（来源健康与核验时间）
  → 带 job_id 引用的求职建议 + 服务端官方链接引用
```

安全边界：模型没有投递、删除、写库或绕过招聘站保护的工具；只读工具调用最多 4 轮、8 次。密钥未配置、用户未同意外发或调用失败时，自动回退到原有确定性 Copilot。工具轨迹会隐去简历正文。

在本机 `.env.career-platform` 配置（不要提交该文件）：

```dotenv
CAREER_AGENT_LLM_ENABLED=true
CAREER_AGENT_LLM_PROVIDER=ollama
CAREER_AGENT_LLM_MODEL=career-qwen3:4b
# Docker 运行时使用 host.docker.internal；直接运行服务时改为 127.0.0.1。
CAREER_AGENT_LLM_BASE_URL=http://host.docker.internal:11434/v1
CAREER_AGENT_LLM_TIMEOUT_SECONDS=120
CAREER_AGENT_LLM_MAX_OUTPUT_TOKENS=128
```

页面仍需要手动勾选“使用已配置的本地模型”。模型调用仅通过本机 Ollama 服务完成；本实现采用受限函数工具、有界调用循环和输出 token 上限，避免长时间无界生成。

## 人工评测集与验收

评测集使用 JSONL：一行一个用例，便于人工标注、Git 审阅和自动读取。模板与规则在 `evals/CAREER_AGENT_EVAL_GUIDE.md`。

建议先完成 30 条开发集、再冻结 30 条测试集，最终扩展到 100 条：

| 类型 | 建议数量 | 重点 |
| --- | ---: | --- |
| 公司精确检索 | 25 | 不出现无关公司 |
| 岗位族意图 | 25 | 不把含 product 的工程岗误判为产品岗 |
| 技能/语义检索 | 20 | 同义表达和项目描述能召回正确岗位 |
| 届别/地点/类型过滤 | 20 | 27 届、校招/实习、城市和实时/历史范围正确 |
| 负例与 Agent 边界 | 10 | 不虚构岗位；历史岗不说成开放；建议有官方引用 |

每次改检索、提示词、模型或生命周期逻辑后，对比 Recall@1/3/10、MRR、公司精确准确率、岗位族意图准确率、Agent 引用完整率和时效表述错误率。

## 校招项目的下一阶段验收顺序

1. 配置真实模型后，用 10 条人工案例验收工具轨迹、官方引用、隐私同意和降级路径。
2. 完成至少 100 条人工标注集并产出基线报告。
3. 继续接入 3–5 个可稳定核验的国内官方源；每个源要有公开契约、fixture、连接器测试和失败保护。
4. 增加管理台：来源最后同步时间、抓取/新增/更新/归档数、失败次数和评测指标趋势。

完成第 1、2 项后，简历可如实表述为“实现基于工具调用的求职规划 Agent”；四项完成后可表述为“构建可评测、可追溯的 Agentic RAG 求职系统”。
