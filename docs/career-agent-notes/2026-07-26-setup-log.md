# CareerAgent 项目初始化记录

日期：2026-07-26

## 目标

将开源项目 `ro-anderson/multi-agent-rag-customer-support` 拉取到本地，作为后续改造为 CareerAgent 的基础代码。

## 计划目录

本地项目目录：

```text
D:\mygit\career-agent
```

项目记录目录：

```text
D:\mygit\career-agent-notes
```

## 已完成

- 已创建项目记录目录 `career-agent-notes`。
- 已保存项目选型记录：`2026-07-26-ai-agent-project-selection.md`。
- 已确认 `D:\mygit` 下目前没有残留的 `career-agent` 半成品目录。

## 当前阻塞

执行以下命令时，命令行环境无法连接 GitHub：

```powershell
git clone https://github.com/ro-anderson/multi-agent-rag-customer-support.git career-agent
```

错误信息：

```text
Failed to connect to github.com port 443
```

这说明当前命令行网络环境暂时无法访问 GitHub，可能与代理、网络策略或 GitHub 连接状态有关。

## 下一步

网络恢复或代理配置完成后，继续执行：

```powershell
cd D:\mygit
git clone https://github.com/ro-anderson/multi-agent-rag-customer-support.git career-agent
cd career-agent
git status
```

如果后续要推到自己的 GitHub，建议不要直接保留原远程作为自己的主远程，而是：

- 保留原项目地址作为 `upstream`。
- 将自己的 GitHub 仓库设置为 `origin`。
- 在自己的仓库中继续改造成 CareerAgent。
